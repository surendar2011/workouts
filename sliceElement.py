# Slice a list to extract specific elements.

''' start: The index where the slice begins (inclusive). If omitted, it starts from the beginning of the list.

stop: The index where the slice ends (exclusive). If omitted, it goes up to the end of the list.

step: The interval between elements in the slice. If omitted, the default step is 1. '''

my_list = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i']
print(my_list[2:5])  # Output: ['c', 'd', 'e']
